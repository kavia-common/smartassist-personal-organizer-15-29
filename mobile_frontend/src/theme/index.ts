import { colors } from './colors';
import { spacing, borderRadius, fontSize, iconSize } from './spacing';
import { typography } from './typography';

export const theme = {
  colors,
  spacing,
  borderRadius,
  fontSize,
  iconSize,
  typography,
};

export type Theme = typeof theme;
